# Server-only Polar endpoint secret. Never prefix this variable with NEXT_PUBLIC_.
POLAR_WEBHOOK_SECRET=

# The deployed private Supabase Edge Function endpoint and its custom bearer secret.
SECURITY_AUDIT_FULFILLMENT_WEBHOOK_URL=
SECURITY_AUDIT_FULFILLMENT_WEBHOOK_SECRET=

# Optional public business contact displayed on the security-audit pages.
NEXT_PUBLIC_SECURITY_AUDIT_CONTACT_EMAIL=
